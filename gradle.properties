org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G
org.gradle.java.home=C:/Program Files/Java/jdk-17

# QShop 依赖 jar。本地默认指向 ../q_shop/<loader>/ 的构建产物；CI 用 -Pqshop_jar=<path> 覆盖。
# 升级 QShop 时只改这一行，不要在 build.gradle 里硬编码版本。
qshop_jar=../../q_shop/forge-1.20.1/build/libs/qshop-forge-1.20.1-1.7.0.jar
